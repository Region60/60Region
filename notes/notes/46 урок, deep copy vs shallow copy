
Два абсолютно одинаковых объекта ни когда не будуьт равны!!!

let a = {
name: 'Max',  //строка
age: 33,   // число
children: {
          daughter: maria,
          daughter2: katya}, // вложенный массив (упорядочный объект)
animals:{
cat:{
      name:"Milka",
      age:10
      },
cat2:{
      name:"Martin",
      age:10
     }
   }
}

let b = a   // Тк "а" это ссылка на объект, то присваивая "b" объект "a" мы присваеваем ссылку на объект.
Получается "а" и "б" это ссылки на один и тот же объект.

Чтобы копировать объект надо поставить фигурные скопки и оператор spread ...

let b = {...a}   // копируется полностью объект, НО (!!!) вложенные объекты не копируются, а копируются ссылки на них
Для того чтобы копировать вложенные объекты, надо менять каждый объект вручную, не забывая про степень вложенности.

letb = {...a};
b.children = {... a.children};
b.animals = {...a.animals};
b.animals.cat = {...a.animals.cat};