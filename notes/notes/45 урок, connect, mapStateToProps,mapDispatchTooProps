
Устанавливаем библиотеку react-redux (npm install react-redux --save)

в Контейнерной компоненте создаем  функцию DialogsContainer, которая возвращает
функцию connect (ипортим ее из react-redux). Ставим два раза скобки,первые скобки
это функция возвращаемая функцией connect, которая возвращает вторую функцию (вторые скобки).

В первые скобки в качестве аргументов записываем функции колбеки с аргументом dispatch и
функцию которая передает часть стейта. Эта та информация, которая раньше передавалась через
пропсы.

Во вторые скобки вписываем компоненту, которой передает информацию контейнерная компонента.
