
создаем  метод dispatch, который передаем через пропсы.
dispatch  проверяет входящие данные и выполняет ту или иную функцию.
В итоге мы передаем не две функции через пропсы, а один метод dispatch
в качестве входящих данных в dispatch  помещаем объект action.

