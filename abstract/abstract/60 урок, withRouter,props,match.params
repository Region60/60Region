
В App прописываем точный адрес в Route у компоненты  profileContainer (знак вопроса указывает роут системе, что параме
тр не является обязательным)
в profileContainer создаем функцию-компоненту WithUrlDataContainerComponent которая является контейнерной
компонентой над ProfileContainer и уже ее оборачиваем функцией connect.
в методе componentDidMount создаем переменную userId ,которой присваиваем значение this.props.match.params.userId
которое сюда прокинула WithRouter и в гет запросе в конце урл прибавляем userId. Получается запрос идет конкретной страницы
пользователя.
также в контейнерной компоненте делаем проверку, если пользователь не пришел, то отображаем пользователя с id - 2.
