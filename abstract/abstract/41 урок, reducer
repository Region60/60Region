
reducer - функция, которая принемает state (свою часть state`a) и action изменяет его копию (если это требуется)
  и возвращает изменненную копию  state

В папке redux создаем файлы dialogs-reducer.ts и profile-reducer.ts Из dispatch переносим логику в эти файлы
в отдеельные функции .которые экспортим по дефолту. Корректируем пути, через аргументы прокидываем state,
каждому reducer свою часть стейта.