

Все элементы файла state упаковываем в один объект.
Переписываем props  в index.js в связис изменениями в state и удаляем ненужные импоты.
Синтаксис методотов: убираем export, let , = , =>.
Когда прокидываем колбеки через пропсы, жестко закрепляем  через .bind(Объект за которым закрепляем)